export function LoginPage() {
  return (
    <div className="page">
      <h1>Connexion — Backoffice</h1>
      <p>Authentification interne (email + mot de passe + 2FA) — à implémenter (tâche Auth).</p>
    </div>
  );
}
